/*
========================================
COLOUR MAP
========================================
* Maps a normalised generator value [-1, 1] to an RGBA colour.
* Future colour modes live here — palettes, gradients, biome maps, etc.
*
* patternExecutor will import from here when colour modes are added.
-----------------------------------------
*/

// Placeholder — grayscale is currently inlined in patternExecutor for performance.
// Future exports:
//   export function palette(value, name)  { ... }
//   export function gradient(value, stops) { ... }
//   export function biome(value)           { ... }
